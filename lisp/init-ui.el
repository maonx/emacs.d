;; Hide tool bar
(tool-bar-mode -1)

;; Hide scroll bar
(scroll-bar-mode -1)

;; Hide menu bar
(menu-bar-mode -1)

;; Change the cursor style
(setq-default cursor-type 'bar)

;; Hide welcome buffer
(setq inhibit-startup-screen t)

(provide 'init-ui)
